# Production API — injected at build time by Vercel
# This file is committed to git as a safe fallback for the production URL.
# Secrets (auth tokens, DB URLs) must NEVER go here.
NEXT_PUBLIC_API_BASE_URL=https://experience-backend-k4aw.onrender.com/api/v1
